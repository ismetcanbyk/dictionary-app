import { StyleSheet } from "react-native";

export default StyleSheet.create({
    iconContainer: {
        position: 'absolute',
        bottom: 20,
        right: 20,
        flexDirection: 'row',
        justifyContent: 'flex-end'
    },
    icon: {
        padding: 18,
        fontSize: 50,
        color: 'black'
    }
});